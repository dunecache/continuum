<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:application="ml.docilealligator.infinityforreddit.fragments.PostFragment">

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh_layout_post_fragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ml.docilealligator.infinityforreddit.customviews.CustomToroContainer
            android:id="@+id/recycler_view_post_fragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingBottom="@dimen/feed_bottom_navigation_clearance"
            android:clipToPadding="false" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!--
      The list's loading, empty and error states. One container, three presentations, so the feed
      never shows a bare blank list while it loads and "nothing here" never looks like a failure.
      Every dimension is a token; the icon and text take their colours from the theme, unlike the
      old fixed illustration this replaced.
    -->
    <LinearLayout
        android:id="@+id/fetch_post_info_linear_layout_post_fragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginHorizontal="@dimen/space_24"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="visible">

        <com.google.android.material.loadingindicator.LoadingIndicator
            android:id="@+id/feed_state_progress_post_fragment"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            tools:visibility="visible" />

        <ImageView
            android:id="@+id/fetch_post_info_image_view_post_fragment"
            android:layout_width="@dimen/feed_state_icon_size"
            android:layout_height="@dimen/feed_state_icon_size"
            android:contentDescription="@null"
            android:importantForAccessibility="no"
            app:tint="?attr/colorOnSurfaceVariant" />

        <TextView
            android:id="@+id/fetch_post_info_text_view_post_fragment"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_16"
            android:gravity="center"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="?attr/font_16"
            android:fontFamily="?attr/font_family"
            tools:text="@string/no_posts" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/feed_state_retry_post_fragment"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_16"
            android:minHeight="@dimen/touch_target_min"
            android:text="@string/retry"
            android:visibility="gone"
            tools:visibility="visible" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
